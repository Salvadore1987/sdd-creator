# {{metadata.name}} — Go stack scaffold

> Generated by `sdd init` on {{generatedAt}} (schemaVersion {{schemaVersion}}).

## Stack snapshot

- **Stack:** Go
- **Architecture:** {{architecture}}
- **Language for prompts/docs:** {{language}}
- **Selected technologies:** {{#each technologies}}`{{this}}`{{#unless @last}}, {{/unless}}{{/each}}

## Recommended baseline

- Go 1.22+, modules layout (`go mod init`)
- chi or gin for HTTP; cobra for CLI
- `testing` + testify + dockertest/testcontainers-go
- zap or zerolog for structured logging
- golang-migrate for schema versioning

## Suggested module layout

```
cmd/<binary>/main.go
internal/
  domain/
  app/        # use cases, ports
  adapters/   # http, db, queue
pkg/          # public helpers (sparingly)
```

## Next steps

1. `sdd brainstorm stakeholders`
2. `sdd brainstorm features`
3. `sdd integrations add`
4. `sdd spec`
