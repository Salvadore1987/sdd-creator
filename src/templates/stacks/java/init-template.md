# {{metadata.name}} — Java stack scaffold

> Generated by `sdd init` on {{generatedAt}} (schemaVersion {{schemaVersion}}).

## Stack snapshot

- **Stack:** Java
- **Architecture:** {{architecture}}
- **Language for prompts/docs:** {{language}}
- **Selected technologies:** {{#each technologies}}`{{this}}`{{#unless @last}}, {{/unless}}{{/each}}

## Recommended baseline

- JDK 21 LTS, Maven or Gradle (Kotlin DSL)
- Spring Boot 3.x or Quarkus 3.x as starter
- JUnit 5 + AssertJ + Mockito + Testcontainers
- SLF4J + Logback (JSON encoder for prod)
- Flyway / Liquibase for schema versioning

## Suggested module layout

```
src/main/java/<group>/<artifact>/
  domain/        # value objects, aggregates, domain services
  application/   # use-case services, ports
  adapters/      # in/out adapters (REST, JPA, Kafka, ...)
  config/        # Spring/Quarkus wiring
src/test/java/...
```

## Next steps

1. `sdd brainstorm stakeholders` — capture personas and ownership.
2. `sdd brainstorm features` — convert use-cases into FR-NNN with acceptance criteria.
3. `sdd integrations add` — register external systems (Postgres, Kafka, Keycloak, ...).
4. `sdd spec` — render the SDD into `docs/SDD.md`.
