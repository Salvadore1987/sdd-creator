# {{metadata.name}} — Rust stack scaffold

> Generated by `sdd init` on {{generatedAt}} (schemaVersion {{schemaVersion}}).

## Stack snapshot

- **Stack:** Rust
- **Architecture:** {{architecture}}
- **Language for prompts/docs:** {{language}}
- **Selected technologies:** {{#each technologies}}`{{this}}`{{#unless @last}}, {{/unless}}{{/each}}

## Recommended baseline

- Stable Rust toolchain (`rustup default stable`)
- axum / actix-web for HTTP; clap for CLI
- `cargo test` + `tokio::test` + testcontainers-rs
- tracing + tracing-subscriber for structured logs
- sqlx / sea-orm for persistence; refinery for migrations

## Suggested module layout

```
crates/
  domain/
  application/
  adapters/
  bin/        # binary entry points
```

## Next steps

1. `sdd brainstorm stakeholders`
2. `sdd brainstorm features`
3. `sdd integrations add`
4. `sdd spec`
