# {{metadata.name}} — Node.js stack scaffold

> Generated by `sdd init` on {{generatedAt}} (schemaVersion {{schemaVersion}}).

## Stack snapshot

- **Stack:** Node.js (TypeScript)
- **Architecture:** {{architecture}}
- **Language for prompts/docs:** {{language}}
- **Selected technologies:** {{#each technologies}}`{{this}}`{{#unless @last}}, {{/unless}}{{/each}}

## Recommended baseline

- Node 20 LTS, TypeScript 5.x (`strict: true`)
- Fastify or NestJS for HTTP; or Commander for CLI
- Jest or Vitest + Supertest + Testcontainers (node)
- Pino for structured JSON logging
- Prisma / Drizzle / Knex for persistence

## Suggested module layout

```
src/
  domain/        # entities, value objects, pure logic
  application/   # use-case services, ports
  adapters/      # http, db, queue, ...
  infra/         # bootstrap, config, DI wiring
tests/{unit,integration,e2e}/
```

## Next steps

1. `sdd brainstorm stakeholders`
2. `sdd brainstorm features`
3. `sdd integrations add`
4. `sdd spec`
