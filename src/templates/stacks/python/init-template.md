# {{metadata.name}} — Python stack scaffold

> Generated by `sdd init` on {{generatedAt}} (schemaVersion {{schemaVersion}}).

## Stack snapshot

- **Stack:** Python
- **Architecture:** {{architecture}}
- **Language for prompts/docs:** {{language}}
- **Selected technologies:** {{#each technologies}}`{{this}}`{{#unless @last}}, {{/unless}}{{/each}}

## Recommended baseline

- Python 3.12, `uv` or `poetry` for env management
- FastAPI / Django / Flask for HTTP; Typer for CLI
- pytest + pytest-asyncio + httpx + pytest-postgresql
- structlog or stdlib `logging` with JSON renderer
- Alembic for SQLAlchemy migrations

## Suggested module layout

```
src/<package>/
  domain/
  application/
  adapters/
  infrastructure/
tests/{unit,integration,e2e}/
```

## Next steps

1. `sdd brainstorm stakeholders`
2. `sdd brainstorm features`
3. `sdd integrations add`
4. `sdd spec`
