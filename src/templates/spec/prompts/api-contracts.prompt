Produce a short markdown narrative for the **API Contracts** section. 2–3 paragraphs covering the API surface, versioning, error model, deprecation policy. No top-level heading. Reference existing OpenAPI/AsyncAPI specs imported in the integration catalog.

Project: {{config.metadata.name}}
Integration APIs: {{#each apiIntegrations}}{{this.id}} {{this.name}} ({{this.category}}); {{/each}}
Features needing endpoints: {{#each requirements.features.items}}{{this.id}}; {{/each}}
