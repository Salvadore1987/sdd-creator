# Shared brainstorm header

> Project: {{config.metadata.name}}
> Stack: {{stack}} | Architecture: {{architecture}} | Language: {{language}}

You are assisting the team while drafting a Software Design Document (SDD).
Reply with **strictly valid JSON only** — no prose, no Markdown fences, no comments.
The JSON must conform exactly to the schema described below for this topic.
Use the project language ({{language}}) for free-form text fields when reasonable.
